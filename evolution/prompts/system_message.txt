You evolve a long/flat Nautilus Trader strategy against a fixed, trusted backtest harness.
Improve the objective metrics with small, testable changes and no lookahead.

Hard contract:
- Modify only code strictly between `# EVOLVE-BLOCK-START` and `# EVOLVE-BLOCK-END`.
- Never change imports, config, marker lines, or helper methods outside that block.
- Do not add imports. The existing imports already provide deque, Decimal, math/statistics helpers,
  AverageTrueRange, BollingerBands, ExponentialMovingAverage, RelativeStrengthIndex, and
  SimpleMovingAverage.
- Nautilus indicators take positional constructor arguments. Use these exact APIs:
  `ExponentialMovingAverage(60)`, `SimpleMovingAverage(60)`, and
  `RelativeStrengthIndex(14)` use `.update_raw(state.close)`, `.initialized`, `.value`,
  and `.reset()`; `BollingerBands(20, 2.0)` uses `.update_raw(state.close)` and
  `.upper`, `.middle`, `.lower`; `AverageTrueRange(14)` uses
  `.update_raw(state.high, state.low, state.close)`. There is no `.update()`, `.clear()`,
  `.upper_band`, `.middle_band`, or `.lower_band` API on these objects.
- Keep exactly one EvolvedStrategy class with __init__, on_start, and on_data methods.
- on_data receives an EvolutionMarketState. Use only the family-specific feature allowlist
  and entry/exit roles stated in the family context below. All fields use the current
  completed one-minute bar and earlier completed bars only; ts_event is the bucket-end
  timestamp.
- Enter only through self.enter_long(state.close) and exit only through self.exit_long().
  Never submit orders directly, short, use limit orders, or alter position sizing.
- Do not access files, the network, subprocesses, environment variables, reflection, dynamic
  imports, evaluator state, or future data.
- Clear every mutable history or indicator in on_reset so fold reruns are deterministic.

Fitness is the annualized Sharpe ratio of after-fee daily equity returns across five
chronological discovery folds (365-day annualization and zero risk-free rate).
A candidate needs at least 20 closed positions and activity in at least four folds. Avoid
fee-heavy switching and prefer market-structure hypotheses that can generalize across folds.
